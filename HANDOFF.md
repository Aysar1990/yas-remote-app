# YAS Remote Pro - Handoff Document
## نظام التحكم عن بعد بالكمبيوتر

---

## 📋 نظرة عامة

نظام تحكم عن بعد بالكمبيوتر من الموبايل عبر الإنترنت، يعمل برابط ثابت بدون الحاجة لـ IP متغير أو Connection Code.

### الهيكل:
```
الموبايل ←→ Relay Server (Render.com) ←→ الكمبيوتر
```

---

## 📁 الملفات والمسارات

### المجلد الرئيسي:
```
C:\Users\aysar\Documents\Remote control\Android App\
```

### هيكل الملفات:
```
Android App\
├── relay-server\              ← سيرفر Node.js (مرفوع على Render.com)
│   ├── server.js              ← الكود الرئيسي v2.0
│   ├── package.json           ← Dependencies
│   └── .gitignore
├── yas-server-relay.py        ← سكربت Python للكمبيوتر
└── yas-remote-pro.html        ← واجهة الموبايل
```

### النظام القديم (للرجوع إليه):
```
C:\Users\aysar\Documents\Remote control\
├── yas-remote-server.py       ← السيرفر القديم (Ngrok)
├── yas-remote-mobile.html     ← الواجهة القديمة
└── ngrok.exe
```

---

## 🔗 الروابط والمعلومات

| العنصر | القيمة |
|--------|--------|
| **Relay Server URL** | `https://yas-remote-relay.onrender.com` |
| **WebSocket URL** | `wss://yas-remote-relay.onrender.com` |
| **GitHub Repo** | `https://github.com/Aysar1990/yas-remote-relay` |
| **Password** | `YasRemote2025` |
| **Render Dashboard** | `https://dashboard.render.com` |

---

## 🚀 طريقة التشغيل

### 1. تشغيل السيرفر على الكمبيوتر:
```powershell
cd "C:\Users\aysar\Documents\Remote control\Android App"
python yas-server-relay.py
```

### 2. النتيجة المتوقعة:
```
========================================
   YAS Remote - Connected!
========================================
   Password: YasRemote2025
========================================
```

### 3. من الموبايل:
- افتح `yas-remote-pro.html`
- أدخل كلمة السر: `YasRemote2025`
- اضغط Connect

---

## ✅ الميزات الحالية

### التحكم:
- ✅ عرض شاشة الكمبيوتر مباشرة
- ✅ تحكم بالماوس (نقر، سحب)
- ✅ نقرة سريعة = كليك يسار
- ✅ ضغط مطول = كليك يمين
- ✅ سحب = تحريك الماوس
- ✅ Pinch to Zoom (من المنتصف + بطيء)
- ✅ لوحة مفاتيح كاملة
- ✅ إرسال نص (Copy + Ctrl+V)
- ✅ PIN Pad للأرقام
- ✅ أوامر النظام (Lock, Sleep, Shutdown, Restart)
- ✅ فتح تطبيقات (Chrome, Claude, Explorer, etc.)
- ✅ مراقبة CPU/RAM/GPU
- ✅ 4 مستويات جودة (Low, Med, High, HD)

### التصميم:
- ✅ ألوان رمادية محايدة هادئة
- ✅ خط Inter الرسمي
- ✅ واجهة نظيفة ومريحة

---

## 🔧 رفع التحديثات على Render

### عند تعديل server.js:
```powershell
cd "C:\Users\aysar\Documents\Remote control\Android App\relay-server"
git add .
git commit -m "وصف التعديل"
git push
```

### التحقق من الإصدار:
افتح: `https://yas-remote-relay.onrender.com/`
- يجب أن يظهر: `{"service":"YAS Remote Relay","version":"2.0"}`

### إذا لم يتحدث تلقائياً:
1. افتح `https://dashboard.render.com`
2. اضغط على `yas-remote-relay`
3. اضغط **Manual Deploy** → **Deploy latest commit**

---

## 📝 الملفات الرئيسية وشرحها

### 1. server.js (Relay Server)
- **المسار:** `relay-server\server.js`
- **الإصدار:** 2.0
- **الوظيفة:** وسيط بين الموبايل والكمبيوتر
- **المصادقة:** بكلمة السر فقط (بدون Connection Code)
- **الميزات:**
  - تسجيل الكمبيوتر بالباسورد
  - ربط العميل بالكمبيوتر
  - نقل الرسائل (Relay)
  - Heartbeat كل 30 ثانية

### 2. yas-server-relay.py (Python Server)
- **المسار:** `yas-server-relay.py`
- **الوظيفة:** يشتغل على الكمبيوتر
- **الإعدادات:**
  ```python
  RELAY_SERVER = "wss://yas-remote-relay.onrender.com"
  PASSWORD = "YasRemote2025"
  ```
- **الميزات:**
  - التقاط Screenshots
  - تنفيذ أوامر الماوس والكيبورد
  - مراقبة النظام
  - فتح التطبيقات

### 3. yas-remote-pro.html (Mobile UI)
- **المسار:** `yas-remote-pro.html`
- **الوظيفة:** واجهة التحكم من الموبايل
- **التابات:**
  - Mouse: كليك، سحب، سكرول، زوم
  - System: Lock, Sleep, Shutdown, Restart
  - Apps: Chrome, Explorer, Claude, etc.
  - Keys: لوحة مفاتيح + PIN Pad

---

## 🎨 الألوان والتصميم

### Color Scheme (Neutral Gray):
```css
--primary: #475569;      /* رمادي أزرق */
--primary-dark: #334155;
--bg: #f8fafc;           /* خلفية فاتحة */
--surface: #ffffff;      /* أبيض */
--surface2: #f1f5f9;     /* رمادي فاتح */
--border: #e2e8f0;       /* حدود */
--text: #0f172a;         /* نص أسود */
--text2: #64748b;        /* نص رمادي */
--success: #22c55e;      /* أخضر */
--error: #ef4444;        /* أحمر */
--warning: #f59e0b;      /* برتقالي */
```

### الخط:
```css
font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
```

---

## 🔜 خطة التطوير القادمة

| الأولوية | المهمة | الوصف |
|----------|--------|-------|
| 1 | تعدد المستخدمين | قائمة المتصلين الحاليين |
| 2 | أجهزة موثوقة | حفظ الأجهزة + إشعار اتصال جديد |
| 3 | نقل ملفات | رفع/تحميل ملفات |
| 4 | مدير ملفات | ملفات حديثة + تصفح بسيط |
| 5 | مراقبة مجلد | File Watcher |
| 6 | جدولة أوامر | تنفيذ أوامر بوقت محدد |
| 7 | OCR | استخراج نص من الشاشة |
| 8 | Wake on LAN | تشغيل الكمبيوتر عن بعد |
| 9 | Tasker Integration | ربط مع Tasker |
| 10 | تطبيق Android | PWA أو APK |

---

## 🐛 حل المشاكل الشائعة

### 1. "Computer not found"
- تأكد أن `yas-server-relay.py` شغال على الكمبيوتر
- تأكد من كلمة السر صحيحة
- تحقق من `/status`: `https://yas-remote-relay.onrender.com/status`

### 2. الشاشة سوداء
- أعد تشغيل السيرفر على الكمبيوتر
- جرب جودة أقل (Low أو Med)

### 3. إرسال النص لا يعمل
- تأكد أن المؤشر في حقل نص
- السيرفر يستخدم Copy + Ctrl+V

### 4. Render لا يتحدث
- اعمل Manual Deploy من Dashboard
- تحقق من version في الرابط الرئيسي

---

## 📦 Dependencies

### Python (الكمبيوتر):
```
pip install websockets pyautogui pyperclip pillow psutil
```

### Node.js (Render):
```json
{
  "dependencies": {
    "ws": "^8.14.2"
  }
}
```

---

## 📞 معلومات إضافية

- **تاريخ الإنشاء:** نوفمبر 2025
- **آخر تحديث:** 29 نوفمبر 2025
- **الإصدار:** 2.0 (بدون Connection Code)
- **المطور:** Aysar

---

## 🔑 ملخص سريع للمحادثة القادمة

```
المشروع: YAS Remote Pro - تحكم عن بعد بالكمبيوتر

الملفات:
- C:\Users\aysar\Documents\Remote control\Android App\

السيرفر:
- Render: https://yas-remote-relay.onrender.com
- GitHub: https://github.com/Aysar1990/yas-remote-relay

التشغيل:
- python yas-server-relay.py
- Password: YasRemote2025

المهام القادمة:
1. تعدد المستخدمين + قائمة المتصلين
2. أجهزة موثوقة + إشعار اتصال جديد
3. نقل ملفات (رفع/تحميل)
4. مدير ملفات + مراقبة مجلد
5. جدولة أوامر + OCR
6. Wake on LAN
7. Tasker Integration
8. تحويل لتطبيق Android (PWA/APK)
```
